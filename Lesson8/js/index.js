$(document).ready(function () {

  // var innerWidth = $('body, html').innerWidth();
  // var innerHeight = $('body, html').innerHeight();

  // var outerWidth = $('body, html').outerWidth();
  // var outerHeight = $('body, html').outerHeight();

  // console.log(outerWidth, outerHeight);

  // var koordinate = $('b').offset();
  // var koordinate = $('b').position();
  // console.log(koordinate);

  // $(document).scroll(function () {
  //   var scrolls = $('html, body').scrollTop();
  //   console.log(scrolls);
  // })

})